{
  "name": "peer-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "peerjs --port $PORT --key peerjs --path /"
  },
  "dependencies": {
    "peer": "^0.5.3"
  }
}
